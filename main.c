#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define FRAME_LENGTH 100 // Defining frame length
#define FRAME_WIDTH 100  // Defining frame width

char frame[FRAME_LENGTH][FRAME_WIDTH]; // Initializing frame

int main()
{

    return 0;
}